# These versions are pinned to exact tested values.
# They have pre-built wheels for Python 3.10, 3.11, 3.12, and 3.13
# — no Rust/C compilation required on any of those versions.
# Tested with: fastapi 0.137.1 + sse-starlette 3.4.4 + pydantic 2.13.4
fastapi==0.137.1
uvicorn==0.49.0
redis==8.0.0
sse-starlette==3.4.4
pydantic==2.13.4
bcrypt==5.0.0
python-dotenv==1.2.2
